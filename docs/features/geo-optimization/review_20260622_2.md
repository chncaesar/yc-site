# Review 2: GEO optimization — final branch review

Date: 20260622
Branch: feature/geo-optimization (commits 1116701..5157a70)

## Summary

All 4 commits across 4 files (AGENTS.md, consts.ts, index.astro, BlogPost.astro) correctly implement the GEO optimization design spec. No critical or major issues found. The implementation is clean, spec-complete, and cross-file consistent. Build + typecheck pass with 0 errors (2 informational hints only). Total: **0 Critical, 0 Major, 3 Minor**.

## Issues

### Critical
(无)

### Major
(无)

### Minor
- [m1] **Indentation inconsistency** — `src/pages/index.astro:38`: the opening `<p class="hero-sub">` tag is indented by 3 tabs while its sibling elements (lines 36, 37, 43, 44) use 4 tabs. The closing `</p>` at line 42 matches the opening (3 tabs), and the text content inside (lines 39–41) correctly uses 4 tabs. Re-align the `<p>` and `</p>` tags to 4 tabs to match siblings.
- [m2] **Missing `is:inline` directive on JSON-LD scripts** — `src/pages/index.astro:19` and `src/layouts/BlogPost.astro:18`: Astro hints (astro:4000) suggest adding `is:inline` explicitly since the `<script>` tags have attributes (`type="application/ld+json"`). Functional impact is zero — Astro already treats them as inline. Adding the directive silences the hints and documents intent.
- [m3] **Article JSON-LD missing `image` field** — `src/layouts/BlogPost.astro:18–30`: when a blog post has `heroImage`, the Article JSON-LD does not include it. Schema.org Article type supports an `image` property, which would enrich GEO entity signals. Consider adding `"image": ${JSON.stringify(new URL(heroImage.src, Astro.site).href)}` when `heroImage` is defined (can be a follow-up enhancement, not blocking).

## Coder Response
<!-- Coder 在修复后填写此节；Reviewer 初次产出时省略 -->

## Verdict: **APPROVED**

所有设计规范 (DESIGN.md) 要求均已实现，跨文件一致性良好。3 个 Minor 问题均非阻塞项，其中 m1/m2 建议顺手修复，m3 可作为后续优化。
